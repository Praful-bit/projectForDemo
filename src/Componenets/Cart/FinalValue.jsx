/* eslint-disable react/prop-types */

// import React from "react";

function FinalValue({ finalValue }) {
  return (
    <div>
      <p className="font-semibold mt-4">Final Value: ${finalValue}</p>
    </div>
  );
}

export default FinalValue;
